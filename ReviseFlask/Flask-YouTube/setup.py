from setuptools import setup, find_packages
setup(
    name='Flask_Youtube',
    version='0.1',
    license='MIT',
    description='Flask extension to learn to write a flask extension.',
    author='Jack Stouffer',
    platforms='any',
    install_requires=['Flask'],
    packages=find_packages()
)
